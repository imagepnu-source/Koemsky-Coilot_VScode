"use client";

// Minimal, no-op header snippet to avoid undefined refs (UIDesignDialog, setShowSettingsDialog).
// This safely compiles and renders nothing. Replace the existing snippets_header_block.tsx with this file.
export default function HeaderSnippetPlaceholder() {
  return null;
}
